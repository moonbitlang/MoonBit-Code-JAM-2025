///|
test "double" {
  let _a = 759.99999999
  let _b = 760.0
  // println(a.to_int())
  // println(b.to_int())
  // println(a == b)
  // println(a.to_int() == b.to_int())
  // println(@math.ceil(a))
  // println(@math.floor(a))
  // println("------------")
  // println(@math.round(a) == b)
}
