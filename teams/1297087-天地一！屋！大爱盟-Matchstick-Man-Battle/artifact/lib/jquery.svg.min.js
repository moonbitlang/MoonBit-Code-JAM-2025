/* minimal stub for jquery.svg plugin to prevent 404. Not actual implementation. */
(function($){
  if (!$) return;
  $.fn.svg = function() { return this; };
  $.fn.svgInit = function() { return this; };
  // provide no-op methods used by older code
  $.fn.draw = function() { return this; };
})(window.jQuery || window.$ || {});
